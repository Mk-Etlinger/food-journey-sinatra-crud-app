%h1.title Welcome #{@user.username}!

- @user.meals.all.each do |meal|
    #dash_box    
        %h3.meal_link
            %a{ href: url("/meal/#{meal.id}") } #{meal.meal_type + '-' + meal.description}
            #dashboard 
                %a{ class: "dash_link_small", href: url("/edit/#{meal.id}") } edit
                %a{ class: "dash_link_small", href: url("/delete/#{meal.id}") } delete
%p
    %a{class: "button", href: url("/new")} Add a meal!


